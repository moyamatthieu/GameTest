# Server configuration
HOST=127.0.0.1
PORT=3001

# Static files directory (defaults to <repoRoot>/dist)
# STATIC_DIR=C:\\path\\to\\repo\\dist

# Persistence data directory (defaults to <repoRoot>/server/data)
# DATA_DIR=C:\\path\\to\\repo\\server\\data

# Limits
MAX_BODY_BYTES=1048576

# Retention
MAX_SNAPSHOTS_PER_CLUSTER=10

# Rate limiting (basic in-memory)
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=60
